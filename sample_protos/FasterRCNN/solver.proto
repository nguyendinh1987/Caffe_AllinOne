train_net: "../../models/frcnn_vgg16/train_val.proto"
base_lr: 0.001
lr_policy: "step"
gamma: 0.1
stepsize: 1000
max_iter: 2000
display: 20
average_loss: 100
momentum: 0.9
weight_decay: 0.0005
# function
snapshot: 1000
# We still use the snapshot prefix, though
snapshot_prefix: "../snapshots/vgg16_faster_rcnn"
iter_size: 2
